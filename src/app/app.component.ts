import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  template: `
    <app-layout>
      <app-characters-page></app-characters-page>
    </app-layout>
  `
})
export class AppComponent {
}
